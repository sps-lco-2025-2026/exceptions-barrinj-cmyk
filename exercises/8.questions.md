Why should InsufficientQuantityException inherit from InventoryException rather than directly from Exception?

Gives users a spectrum of specificity; catch all inventory errors in one block, or catch each type separately for precision

Could a caller catch both ItemNotFoundException and InsufficientQuantityException with a single catch block? How?

Yes: catch (InventoryException ex) matches both ItemNotFoundException and InsufficientQuantityException through inheritance

When would you choose to catch Exception rather than a specific type?

At the top of the call stack in Main or request handler

What's the purpose of the (string message, Exception inner) constructor? When would you use inner?

Lets you wrap a low-level exception in a high-level one without losing the original

Discuss when you might explicitly deploy the throw ex pattern.

When hiding implementation details 
